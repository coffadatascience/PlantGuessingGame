<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PlantGuessingGame.Views.PlantProblemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlantGuessingGame.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!--<Page.DataContext>
        <viewModels:PlantProblemsViewModel />
    </Page.DataContext>-->

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Welcome Label -->
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Text="Welcome to WinUI!"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Margin="0,0,0,20" />

        <!-- Plant Classification Label -->
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            VerticalAlignment="Center"
            Text="Classification:"
            Style="{StaticResource DefaultTextBoxStyle}" />

        <!-- Plant Classification ComboBox -->
        <ComboBox
            Grid.Row="1"
            Grid.Column="1"
            ItemsSource="{x:Bind ViewModel.PlantProblems}"
            SelectedItem="{x:Bind ViewModel.SelectedPlantProblem, Mode=TwoWay}"
            DisplayMemberPath="Name"/>

        <!--Style="{StaticResource AttributeComboxValueStyle}"--> 
    </Grid>
</Page>
